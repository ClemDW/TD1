{
  "name": "web",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run typecheck && bun run --hot src/index.ts",
    "typecheck": "bun x tsc --noEmit" 
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  }
}
